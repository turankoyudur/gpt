# DayZ Web Panel
# Copy this file to ".env" (install.bat does this automatically) and adjust if needed.

# Web panel port
PORT=3000

# SQLite database (relative to prisma/schema.prisma)
DATABASE_URL="file:../data/app.db"

# Default server instance id (for future multi-instance support)
INSTANCE_ID=default

# Optional: Steam Web API key for fetching workshop details (highly recommended)
# You can request a key from Valve/Steam Web API.
# STEAM_WEB_API_KEY=
